
export const buscarNumero = (data, num) => {
  return 'example';
};

export const buscarTipo = (data, type) => {
  return 'example';
};

export const buscarRaridade = (date, raridade) => {
  return 'OMG';
};

export const buscarEstatisticas = (data, ) => {
  return 'example';
};

export const buscarOrdemCrescente = (data, ) => {
  return 'example';
};

export const buscarOrdemDecrescente = (data, ) => {
  return 'example';
};

//funcões que faz os filtros e ordenação A-Z
